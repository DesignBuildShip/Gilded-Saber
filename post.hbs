{{!< default}} {{! Get the default layout }}

<main role="main">
  
  {{#post}}

    <article role="article" class="{{post_class}}">

      <header class="post-header">

        <a href="{{url}}"><h1 class="post-title">{{title}}</h1></a>

        <aside class="post-meta">
          <p class="meta">Published <span><time class="post-date">{{date format='MMMM DD, YYYY'}}</time></span> by <span>{{#author}}<a href="{{url}}">{{name}}</a>{{/author}}</span> on <span>{{tags}}</span></p>
        </aside>

      </header>
      
      {{#if image}}
        <figure class="post-image">
          {{image}}
        </figure>
      {{/if}}

      <section class="post-content">
        {{content}}
      </section>

    </article>
    
    <aside class="post-author media">
      {{#author}}

        {{#if image}}
          <img class="media-img author-image" src="{{image}}" alt="A picture of {{name}}">
        {{/if}}

        <div class="media-body">
          <h5 class="author-name">About {{name}}</h5>

          {{#if bio}}
            <p class="author-bio">{{bio}} <span class="author-more"><a href="{{url}}">See more articles by {{name}} »</a></span></p>
          {{/if}}

        {{!--
          {{#if website}}
             <p class="author-website">You can learn more about what {{name}} does at <a href="{{website}}">{{website}}</a></p>
          {{/if}}
         --}}

        </div>
      {{/author}}
    </aside>

    {{! Grab Disqus from partials }}
    {{> disqus}}

  {{/post}}

</main>
